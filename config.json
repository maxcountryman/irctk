import voxbot
import gevent

SETTINGS = {
    'server': 'sagan.voxinfinitus.net', 
    'nick': 'Kaa', 
    'realname': 'Kaa the Python', 
    'port': 6697, 
    'ssl': True, 
    'channels': ['#testing'], 
    'plugins': ['test']
    }

try:
    jobs = [gevent.spawn(voxbot.bot, SETTINGS)]
    gevent.joinall(jobs)
finally:
    gevent.killall(jobs)
